# 试卷分析 RAG 系统项目规则

## 项目背景
这是一个基于 AI 的高中试卷分析系统。流程：用户拍照 -> n8n 接收 -> 视觉模型定位题目坐标 -> Python 切图处理 -> DeepSeek 诊断分析。

## 技术栈规范
- **后端语言**: Python 3.10+
- **核心库**: Pillow (图像处理), Requests (API 调用), Pandas (数据处理)
- **集成工具**: n8n (工作流引擎), Docker (部署)
- **AI 模型**: Qwen-2-VL (定位), DeepSeek-V3 (诊断), PaddleOCR (可选本地识别)

## 编码原则 (Vibe Coding 指导)
1. **坐标处理**: 始终假设坐标是归一化 [0, 1000] 格式，需换算像素。
2. **切图逻辑**: 切图时自动增加 1% 的 Padding 留白，防止文字被切断。
3. **分栏意识**: 处理高中试卷时，优先考虑垂直中线（x=500 左右）的分栏逻辑。
4. **错误处理**: 所有的 API 调用必须包含 retry 逻辑和详细的日志记录。

## 目录结构
- `/scripts`: 存放被 n8n 调用的 Python 脚本
- `/output`: 临时存放切好的题目图片
- `/docs`: 存放考纲 RAG 知识库（Markdown 格式）